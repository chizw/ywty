---
import BaseLayout from '../../../layouts/BaseLayout.astro'
import { TicketDetailPage } from '../../../components/react/dashboard/TicketDetailPage.tsx'

const { id } = Astro.params
const ticketId = Number(id)
---

<BaseLayout title="工单详情">
  <TicketDetailPage id={ticketId} client:load />
</BaseLayout>
