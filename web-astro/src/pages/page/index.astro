---
// 自定义单页（静态壳）：已知 slug（about/terms/privacy 等）构建时预渲染，
// 未知的 slug 由 Web 服务器回退到本页，客户端拉取内容
import BaseLayout from '../../layouts/BaseLayout.astro'
import PublicLayout from '../../layouts/PublicLayout.astro'
import PageView from '../../components/vue/page/PageView.vue'
---

<BaseLayout title="页面">
  <PublicLayout>
    <PageView client:load />
  </PublicLayout>
</BaseLayout>
